// Carrega todos os controladores Stimulus manualmente
// Como usamos javascript_include_tag "controllers/index", este script será carregado

// Registra os controladores no Stimulus
// Exemplo: Se você tem um controlador chamado "hello_controller.js", ele será registrado aqui
document.addEventListener("DOMContentLoaded", () => {
  console.log("Stimulus controllers index loaded");

  // Registra todos os controladores manualmente
  // Você pode adicionar mais controladores conforme necessário
  // Exemplo: Stimulus.register("hello", HelloController);
});
